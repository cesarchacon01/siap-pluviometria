import os, json, urllib.request
from datetime import datetime, timezone

ASSET_UID = "avkyXRNMhnGTPqmNwdj3C4"
BASE = "https://eu.kobotoolbox.org"
TOKEN = os.environ["KOBO_API_TOKEN"]

def get_json(url):
    req = urllib.request.Request(url, headers={
        "Authorization": f"Token {TOKEN}",
        "Accept": "application/json"
    })
    with urllib.request.urlopen(req, timeout=60) as r:
        return json.load(r)

# Para cada campo de salida, se listan los posibles nombres de pregunta del
# XLSForm en orden de PRIORIDAD: primero el campo calculado (nombre limpio,
# ya traducido por Kobo), y solo si no existe, se usa el campo crudo del
# select_one (que trae el código interno / slug, no la etiqueta).
FIELD_PRIORITY = {
    "municipio":   ["municipio", "municipio_sel"],
    "comunidad":   ["comunidad_nombre", "comunidad"],
    "caserio":     ["caserio_nombre", "caserio"],
    "responsable": ["responsable", "informante"],
    "fecha":       ["fecha"],
    "mm":          ["mm", "lluvia", "precipit"],
}

def pick_exact(r, candidates):
    """Busca coincidencia EXACTA de nombre de campo (ignorando mayúsculas
    y un
